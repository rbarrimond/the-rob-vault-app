[pytest]
# Add the project root to the Python path so tests can import modules from the root
pythonpath = .
# Optional: show more verbose output
addopts = -v
# Optional: ignore __pycache__
norecursedirs = __pycache__
